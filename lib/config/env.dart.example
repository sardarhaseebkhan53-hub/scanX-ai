/// TEMPLATE for local-only secrets.
///
/// Copy this file to `lib/config/env.dart` and fill in your real keys.
/// `env.dart` is gitignored so secrets are never committed.
library;

class EnvSecrets {
  const EnvSecrets._();

  /// Groq API key (https://console.groq.com/keys).
  static const String groqApiKey = 'YOUR_GROQ_API_KEY_HERE';
}
